[package]
name = "middleware"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4.11.0"
actix-web-httpauth = "0.8.2"
jsonwebtoken = "9.3.1"

# Crate compartido
shared = { path = "../shared" }

[lib]
path = "src/lib.rs"